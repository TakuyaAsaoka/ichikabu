# 管理UI（銘柄・保有の登録）実装計画

> **作業する人へ**: この計画は `superpowers:subagent-driven-development` または `superpowers:executing-plans` でタスクごとに実行する。手順は `- [ ]` のチェックボックスで進捗を追う。

**ゴール**: 銘柄（`stock`）と保有（`holding`）を管理画面から登録できるようにする。Issue #7 でイベントを登録できる状態を作ることが目的。

**方針**: 画面は Next.js の Server Component、登録は Server Action。DB への INSERT は `src/db/register.ts` に切り出し、実際の PostgreSQL に対して Vitest で検証する。登録の失敗は事前確認せず INSERT し、PostgreSQL が返した制約違反を日本語に訳す。

**使うもの**: Next.js 16（App Router）、React 19（`useActionState`）、Drizzle ORM、PostgreSQL、Better Auth、Tailwind v4、Vitest、Biome

**設計書**: [`docs/records/specs/2026-08-09-6-admin-ui-design.md`](../specs/2026-08-09-6-admin-ui-design.md)

## 全体の制約

すべてのタスクに以下が適用される。

- worktree の中で作業する。メインの checkout では作業しない
- **コマンドを流す場所**: `pnpm` と `docker compose` は `server/`、`git` と `gh` は worktree のルート。この計画に書いた `git add` のパスは worktree のルートから見たもの
- コードコメント・テストケース名・コミットメッセージは**日本語**。ファイル名・ディレクトリ名は英語
- 整形は `server/biome.json` に従う。**インデントはスペース2**、文字列はダブルクォート、セミコロンあり（別プロダクト kabu-legends のタブインデントには合わせない）
- `openapi.yaml` に登録系のパスを足さない。管理UIは Next.js 内で完結する（設計書 §12）
- `server/src/generated/` はコミットしない
- 品質ゲート（`CLAUDE.md`）は `server/` で次を流す。開発用DBが起動していることが前提
  ```
  pnpm install && pnpm gen && pnpm build && pnpm test:run && pnpm typecheck && pnpm lint
  ```
- **warning も 0 件にする**。既存の問題であっても直す。`as` キャストで回避しない
- `tailwind.config.js` は作らない（Tailwind v4 では不要）
- コミットは各タスクの最後に1回。push はタスク5が終わるまでしない

## 作るファイルと責務

| ファイル | 新規/変更 | 責務 |
|---|---|---|
| `server/postcss.config.mjs` | 新規 | Tailwind v4 の PostCSS プラグイン指定 |
| `server/biome.json` | 変更 | `css.parser.tailwindDirectives` を足す |
| `server/package.json` | 変更 | devDependency に `tailwindcss` と `@tailwindcss/postcss` |
| `server/app/globals.css` | 新規 | `@import "tailwindcss";` と色5つ |
| `server/app/layout.tsx` | 新規 | ルートレイアウト（`html` / `body` / 本文の幅） |
| `server/src/auth.ts` | 変更 | `plugins` に `nextCookies()` を最後として足す |
| `server/app/actions.ts` | 新規 | Server Action 3つ。認証の確認・FormData の読み取り・`register.ts` の呼び出し |
| `server/app/signin/page.tsx` | 新規 | サインイン画面 |
| `server/app/signin/signin-form.tsx` | 新規 | サインインのフォーム（`useActionState`） |
| `server/src/db/violation.ts` | 新規 | pg のエラーから制約名を取り出す（`test/helpers.ts` から移す） |
| `server/test/helpers.ts` | 変更 | `violatedConstraint` を `src/db/violation.ts` から取り込む |
| `server/src/db/register.ts` | 新規 | `createStock` / `createHolding`。制約違反の日本語化 |
| `server/src/db/register.test.ts` | 新規 | 実際の PostgreSQL に対するテスト8件 |
| `server/app/page.tsx` | 新規 | 管理画面。一覧の取得と表示 |
| `server/app/stock-form.tsx` | 新規 | 銘柄の登録フォーム（`useActionState`） |
| `server/app/holding-form.tsx` | 新規 | 保有の登録フォーム（`useActionState`） |

設計書 §4 のファイル一覧に対し、フォームの3ファイル（`signin-form.tsx` / `stock-form.tsx` / `holding-form.tsx`）が増えている。`useActionState` は Client Component でしか使えないため、フォームだけを `"use client"` に切り出す必要があるため。設計書 §8「共通のフォーム部品を先に作らない」に従い、3つを別々に書く。括り出すかどうかはタスク4の最後で判断する。

---

## タスク1: Issue #6 の本文からテーマ関連を外す

コードは触らない。Issue の本文と完了条件を設計書 §2 の決定に合わせる。先にやる理由は、古い完了条件のまま実装すると、作らないと決めたテーマ画面が「未完了」として残り続けるため。

**ファイル**: なし（GitHub の Issue 本文）

- [ ] **手順1: 現在の本文を控える**

```bash
gh issue view 6 --json body -q .body > /tmp/issue-6-before.md
cat /tmp/issue-6-before.md
```

- [ ] **手順2: 本文を書き換える**

次の内容で `gh issue edit 6 --body-file` を使って差し替える。変更点は3つ。①「やること」から `theme` と `theme_stock` の行を消す、②完了条件から「保有・テーマ・テーマ所属も同様に」を「保有も同様に」に直す、③「外したもの」の節を足す。

```markdown
## 目的

イベント登録（Issue 7）の前提データ（銘柄・保有）を画面から入れられるようにする。人力の管理UIは製品の中身である（§1）。

## やること

- Next.js の管理UIとして以下の登録画面を作る（§6。認証は Cookie セッション、§9）:
  - `stock`: market(JP/US)・ticker・name・fiscal_month（JPのみ。§4.1）
  - `holding`: user × stock（株数・単価は持たない。§3）
- UNIQUE 制約違反（stock の (market, ticker)、holding の (user, stock)。§4.1）を画面上のエラー表示にする
- 登録処理のテストを Vitest で書く（テストケース名は日本語）

## 完了条件

- [ ] 管理UIから銘柄を1件登録すると、DBの `stock` に行が入る
- [ ] 同一 (market, ticker) の銘柄を再登録すると、画面にエラーが表示される（500で落ちない）
- [ ] 保有も同様に、登録成功でDBに行が入り、UNIQUE 違反でエラー表示になる
- [ ] 英字入りティッカー（例: `130A`）が登録できる（§4.2「ticker は文字列」の検証）
- [ ] US銘柄を決算月なしで登録できる（§4.1）
- [ ] 未認証で管理UIにアクセスするとサインイン画面に誘導される
- [ ] server 側ゲートが exit 0

## このIssueから外したもの

`theme` と `theme_stock` の登録画面は別Issueに切り出す。全体設計書 §5.1 で出典を「各社IRページ1つだけ」に絞った結果、テーマイベントが登録できないため、テーマの登録画面を作っても紐づけ先のイベントが無く動作確認もできない。出典を増やしてテーマイベントが登録できるようになった時点で作る。

## 依存

Issue 2

## 補足

- 一覧・編集・削除は最小限でよい。この Issue の目的は「Issue 7 でイベントを登録できる状態を作ること」。作り込みは価値検証の後
- 画面の見た目に凝らない。利用者=運用者=自分（§12）
- fiscal_month の入力は JP のみ想定（§4.1）。US 銘柄で空にできること

---
根拠: [設計書](docs/records/specs/2026-08-02-1-ichikabu-design.md)、[管理UI設計書](docs/records/specs/2026-08-09-6-admin-ui-design.md)
```

- [ ] **手順3: 反映されたことを確かめる**

```bash
gh issue view 6 | grep -c "theme"
```

期待: `0`（`theme` の文字が本文から消えている）

- [ ] **手順4: theme / theme_stock の別Issueを作る**

```bash
gh issue create --title "[feat] 管理UI: テーマとテーマ所属の登録" --body "$(cat <<'EOF'
## 目的

`theme` と `theme_stock` を画面から登録できるようにする。Issue #6 から切り出したもの。

## いつ着手するか

**今は着手しない。** 全体設計書 §5.1 で出典を「各社IRページ1つだけ」に絞っているため、テーマイベントが登録できない。テーマを登録しても紐づけ先のイベントが無く、動作確認ができない。

着手できる条件: §5.1 の「出典を増やすときの手順」を踏んで、テーマイベントの日付を確認できる出典が「使う」になること。

## やること

- `theme`: 名前
- `theme_stock`: theme × stock
- UNIQUE 制約違反（theme の名前、theme_stock の (theme, stock)）を画面上のエラー表示にする

## 依存

Issue 6（管理UIの土台・サインイン画面・登録処理の書き方）

---
根拠: [設計書](docs/records/specs/2026-08-02-1-ichikabu-design.md) §5.1、[管理UI設計書](docs/records/specs/2026-08-09-6-admin-ui-design.md) §2
EOF
)"
```

- [ ] **手順5: 設計書に新しいIssue番号を書く**

`docs/records/specs/2026-08-09-6-admin-ui-design.md` の §2 と §12 にある「別Issueに切り出す」を、手順4 で作った Issue 番号（`#NN`）に書き換える。

```bash
grep -n "別Issueに切り出す" docs/records/specs/2026-08-09-6-admin-ui-design.md
```

- [ ] **手順6: コミット**

```bash
git add docs/records/specs/2026-08-09-6-admin-ui-design.md
git commit -m "[docs] テーマ登録の切り出し先Issue番号を設計書に書く"
```

---

## タスク2: Tailwind の導入とサインイン画面

**成果物**: `http://localhost:3000/signin` でサインインでき、誤ったパスワードならフォームの下にエラーが出る。

Tailwind の設定をこのタスクに含める理由は、`globals.css` を読み込む `layout.tsx` が無いと Tailwind が動いているかを確かめられないため。`biome.json` の変更を先にやる理由は、無いと `pnpm lint`（品質ゲート）が `globals.css` の `@theme` を未知の記法として落とし、以降の作業が全部止まるため。

**ファイル**:
- 新規: `server/postcss.config.mjs`
- 新規: `server/app/globals.css`
- 新規: `server/app/layout.tsx`
- 新規: `server/app/actions.ts`
- 新規: `server/app/signin/page.tsx`
- 新規: `server/app/signin/signin-form.tsx`
- 変更: `server/package.json`（devDependency 2つ）
- 変更: `server/biome.json`
- 変更: `server/src/auth.ts`

**このタスクが作る名前**（後続のタスクが使う）:
- `signIn(previous: string | null, formData: FormData): Promise<string | null>` … `app/actions.ts` の Server Action。成功で `redirect("/")`、失敗で日本語のエラー文を返す

- [ ] **手順1: Tailwind を入れる**

```bash
pnpm add -D tailwindcss @tailwindcss/postcss
```

- [ ] **手順2: `server/postcss.config.mjs` を作る**

```js
/**
 * Tailwind v4 は PostCSS プラグイン1つで動く。
 * v3 までの tailwind.config.js は要らない（色などの設定は app/globals.css に書く）
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

- [ ] **手順3: `server/app/globals.css` を作る**

```css
@import "tailwindcss";

/*
 * 管理画面で使う色だけを決める（設計書 §8）。
 * 利用者=運用者=自分1人（全体設計書 §12）なので、明暗の切り替えも配色の設定も持たない
 */
@theme {
  --color-background: #ffffff;
  --color-foreground: #1a1a1a;
  --color-muted: #6b7280;
  --color-border: #d1d5db;
  --color-error: #b91c1c;
}
```

- [ ] **手順4: `server/biome.json` に Tailwind の記法を教える**

`"linter"` の直前に `"css"` を足す。これが無いと `pnpm lint` が `@theme` で落ちる。

```json
  "css": {
    "parser": {
      "tailwindDirectives": true
    }
  },
```

- [ ] **手順5: `pnpm lint` が通ることを確かめる**

```bash
pnpm lint
```

期待: exit 0。`@theme` や `@import` に関するエラーが出ないこと。

もし `noUnknownAtRules` のエラーが残る場合は、`biome.json` の末尾に次の `overrides` を足してもう一度流す（別プロダクト kabu-legends の `biome.json` が同じ例外を置いている）。

```json
  "overrides": [
    {
      "includes": ["**/*.css"],
      "linter": {
        "rules": {
          "suspicious": {
            "noUnknownAtRules": "off"
          }
        }
      }
    }
  ]
```

- [ ] **手順6: `server/app/layout.tsx` を作る**

```tsx
import type { Metadata } from "next";
import type { ReactNode } from "react";
import "./globals.css";

export const metadata: Metadata = {
  title: "イチカブ 管理",
};

/**
 * ルートレイアウト。App Router は html と body をここで書くことを求める。
 * 画面が2枚しかないためナビゲーションは置かない（設計書 §3）
 */
export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="ja">
      <body className="bg-background text-foreground">
        <main className="mx-auto flex max-w-3xl flex-col gap-10 p-6">
          {children}
        </main>
      </body>
    </html>
  );
}
```

- [ ] **手順7: `server/src/auth.ts` に `nextCookies()` を足す**

`import` に1行足し、`plugins` を書き換える。

```ts
import { nextCookies } from "better-auth/next-js";
```

```ts
  // Web管理UIは Cookie セッション、iOS は Bearer トークン（設計書 §9）。
  // nextCookies は Server Action が返した Set-Cookie を next/headers の cookies() に
  // 書き移す。ライブラリが「配列の最後であること」を求めるため、bearer より後に置く
  plugins: [bearer(), nextCookies()],
```

- [ ] **手順8: `server/app/actions.ts` を作る**

```ts
"use server";

import { APIError } from "better-auth/api";
import { headers } from "next/headers";
import { redirect } from "next/navigation";
import { auth } from "../src/auth";

/**
 * メールアドレスとパスワードでサインインする。
 * 戻り値は失敗したときのエラー文で、useActionState の状態になる。
 * 成功したときは redirect が制御を移すため値を返さない
 */
export async function signIn(
  _previous: string | null,
  formData: FormData,
): Promise<string | null> {
  const email = String(formData.get("email") ?? "");
  const password = String(formData.get("password") ?? "");

  try {
    await auth.api.signInEmail({
      body: { email, password },
      headers: await headers(),
    });
  } catch (error) {
    // Better Auth はパスワード誤りを APIError で投げる（設計書 §7 B）。
    // それ以外は原因が分からないので投げ直す
    if (error instanceof APIError) {
      return "メールアドレスまたはパスワードが違います";
    }
    throw error;
  }

  // redirect は例外を投げて制御を移す仕組みのため、上の catch の外に置く。
  // 中に入れると成功したのにエラー表示になる（設計書 §7 C）
  redirect("/");
}
```

- [ ] **手順9: `server/app/signin/signin-form.tsx` を作る**

```tsx
"use client";

import { useActionState } from "react";
import { signIn } from "../actions";

/** サインインのフォーム。useActionState を使うため Client Component にする */
export function SignInForm() {
  const [error, formAction, pending] = useActionState(signIn, null);

  return (
    <form action={formAction} className="flex flex-col gap-3">
      <label className="flex flex-col gap-1">
        メールアドレス
        <input
          type="email"
          name="email"
          required
          className="rounded border border-border p-2"
        />
      </label>
      <label className="flex flex-col gap-1">
        パスワード
        <input
          type="password"
          name="password"
          required
          className="rounded border border-border p-2"
        />
      </label>
      <button
        type="submit"
        disabled={pending}
        className="rounded border border-border p-2 disabled:opacity-50"
      >
        {pending ? "送信中" : "サインイン"}
      </button>
      {error && <p className="text-error">{error}</p>}
    </form>
  );
}
```

- [ ] **手順10: `server/app/signin/page.tsx` を作る**

```tsx
import { headers } from "next/headers";
import { redirect } from "next/navigation";
import { auth } from "../../src/auth";
import { SignInForm } from "./signin-form";

/** サインイン画面。サインイン済みで開いたら管理画面へ戻す */
export default async function SignInPage() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (session) {
    redirect("/");
  }

  return (
    <>
      <h1 className="text-xl font-bold">イチカブ 管理</h1>
      <SignInForm />
    </>
  );
}
```

- [ ] **手順11: ビルドと型検査が通ることを確かめる**

```bash
pnpm build && pnpm typecheck && pnpm lint
```

期待: 3つとも exit 0。

- [ ] **手順12: 既存テストが壊れていないことを確かめる**

`nextCookies()` を足した影響を見る。設計書 §6 のとおり壊れないはずだが、実際に流して確かめる。

```bash
pnpm test:run
```

期待: 全パス。落ちた場合は `nextCookies()` の追加が原因なので、設計書 §6 の記述と実際の挙動が食い違っている。先に進まず報告する。

- [ ] **手順13: 画面で確かめる**

```bash
pnpm dev
```

ブラウザのシークレットウィンドウで `http://localhost:3000/signin` を開く。

| 操作 | 期待 |
|---|---|
| 画面を開く | メールアドレスとパスワードの欄が出る |
| 誤ったパスワードで送信 | 「メールアドレスまたはパスワードが違います」がボタンの下に出る |
| `.env.local` の `SEED_USER_EMAIL` / `SEED_USER_PASSWORD` で送信 | `/` に移る。**この時点では `/` は 404 になる**（`page.tsx` はタスク4で作る） |
| もう一度 `/signin` を開く | `/` に飛ばされる（404 になる。サインイン済みの判定が効いている証拠） |

サインインが 403（`MISSING_OR_NULL_ORIGIN` / `INVALID_ORIGIN`）で失敗した場合は、設計書 §6 の「Origin 検査は問題にならない」が成り立っていない。先に進まず報告する。

- [ ] **手順14: コミット**

```bash
git add server/package.json server/pnpm-lock.yaml server/postcss.config.mjs server/biome.json server/app/globals.css server/app/layout.tsx server/app/actions.ts server/app/signin server/src/auth.ts
git commit -m "[feat] Tailwind を入れてサインイン画面を作る"
```

---

## タスク3: 登録処理と、実際のPostgreSQLに対するテスト

**成果物**: `createStock` / `createHolding` が動き、制約違反が日本語のエラー文で返る。テスト8件が通る。

テストを先に書く。

**ファイル**:
- 新規: `server/src/db/violation.ts`
- 新規: `server/src/db/register.ts`
- 新規: `server/src/db/register.test.ts`
- 変更: `server/test/helpers.ts`

**このタスクが使う名前**（タスク2以前から）: なし（DB層だけで完結する）

**このタスクが作る名前**（タスク4が使う）:
- `type StockInput = { market: "JP" | "US"; ticker: string; name: string; fiscalMonth: number | null }`
- `createStock(input: StockInput): Promise<string | null>` … 成功で `null`、制約違反で日本語のエラー文
- `createHolding(userId: string, stockId: number): Promise<string | null>` … 同上

- [ ] **手順1: `violatedConstraint` を `src/db/` に移す**

`server/src/db/violation.ts` を作る。中身は `server/test/helpers.ts` にある同名の関数をそのまま移したもの。

```ts
/** pg のエラーから違反した制約名を取り出す。Drizzle は元のエラーを cause に包む */
export function violatedConstraint(error: unknown): string | undefined {
  let current: unknown = error;
  while (current instanceof Error) {
    const { constraint } = current as Error & { constraint?: unknown };
    if (typeof constraint === "string") return constraint;
    current = current.cause;
  }
  return undefined;
}
```

- [ ] **手順2: `server/test/helpers.ts` を移した先に向ける**

`violatedConstraint` の定義（JSDoc を含む8行）を消し、先頭の import に足す。

```ts
import { violatedConstraint } from "../src/db/violation";
```

- [ ] **手順3: 既存テストが通ることを確かめる**

移動だけで挙動を変えていないことを確かめる。

```bash
pnpm test:run
```

期待: 全パス。

- [ ] **手順4: `server/src/db/register.test.ts` を書く（この時点では落ちる）**

```ts
import { eq } from "drizzle-orm";
import { beforeEach, describe, expect, it } from "vitest";
import { resetDatabase } from "../../test/helpers";
import { db } from ".";
import { createHolding, createStock } from "./register";
import { holding, stock } from "./schema";
import { seedUser } from "./seed-user";

const TOYOTA = {
  market: "JP",
  ticker: "7203",
  name: "トヨタ自動車",
  fiscalMonth: 3,
} as const;

beforeEach(resetDatabase);

describe("createStock", () => {
  it("銘柄を登録するとDBに行が入る", async () => {
    expect(await createStock({ ...TOYOTA })).toBeNull();

    const rows = await db.select().from(stock);
    expect(rows).toHaveLength(1);
    expect(rows[0].ticker).toBe("7203");
    expect(rows[0].fiscalMonth).toBe(3);
  });

  it("同じ市場とティッカーをもう一度登録するとエラー文が返る", async () => {
    await createStock({ ...TOYOTA });

    expect(await createStock({ ...TOYOTA, name: "別名で再登録" })).toBe(
      "その市場のティッカーは登録済み",
    );
    expect(await db.select().from(stock)).toHaveLength(1);
  });

  it("英字入りのティッカーを登録できる", async () => {
    // 全体設計書 §4.2「ticker は文字列」の検証。数値型だと 130A が入らない
    expect(
      await createStock({
        market: "JP",
        ticker: "130A",
        name: "英字入りティッカーの銘柄",
        fiscalMonth: 12,
      }),
    ).toBeNull();
  });

  it("全角のティッカーはエラー文が返る", async () => {
    // 全角の「７２０３」が半角の「7203」と別銘柄として登録されるのを防ぐ
    expect(await createStock({ ...TOYOTA, ticker: "７２０３" })).toBe(
      "ティッカーは半角の数字・英大文字・ピリオド・ハイフンだけ使える",
    );
  });

  it("US銘柄に決算月を入れるとエラー文が返る", async () => {
    // 決算月はJP銘柄のみ。US銘柄に入るとJPの休場日カレンダーで計算した
    // 権利確定日がUS銘柄に出てしまう（全体設計書 §4.1）
    expect(
      await createStock({
        market: "US",
        ticker: "AAPL",
        name: "Apple",
        fiscalMonth: 9,
      }),
    ).toBe("決算月はJP銘柄にだけ入れられる");
  });

  it("US銘柄は決算月なしで登録できる", async () => {
    expect(
      await createStock({
        market: "US",
        ticker: "AAPL",
        name: "Apple",
        fiscalMonth: null,
      }),
    ).toBeNull();
  });
});

describe("createHolding", () => {
  let userId: string;
  let stockId: number;

  beforeEach(async () => {
    // holding.user_id は Better Auth の user への外部キー。
    // resetDatabase が user も消すため、毎回作り直す（設計書 §7 D）
    ({ userId } = await seedUser(
      "dev@example.com",
      "correct-horse-battery-staple",
    ));
    await createStock({ ...TOYOTA });
    const [row] = await db
      .select()
      .from(stock)
      .where(eq(stock.ticker, TOYOTA.ticker));
    stockId = row.id;
  });

  it("保有を登録するとDBに行が入る", async () => {
    expect(await createHolding(userId, stockId)).toBeNull();

    const rows = await db.select().from(holding);
    expect(rows).toHaveLength(1);
    expect(rows[0].stockId).toBe(stockId);
  });

  it("同じ銘柄をもう一度保有に登録するとエラー文が返る", async () => {
    await createHolding(userId, stockId);

    expect(await createHolding(userId, stockId)).toBe(
      "その銘柄はすでに保有に登録済み",
    );
    expect(await db.select().from(holding)).toHaveLength(1);
  });
});
```

- [ ] **手順5: テストが落ちることを確かめる**

```bash
pnpm vitest run src/db/register.test.ts
```

期待: FAIL。`./register` が見つからないという内容。

- [ ] **手順6: `server/src/db/register.ts` を書く**

```ts
import { db } from ".";
import { holding, stock } from "./schema";
import { violatedConstraint } from "./violation";

/**
 * 制約違反を画面に出す日本語にする（設計書 §5）。
 * 制約名は server/drizzle/0000_simple_blacklash.sql の実物
 */
const MESSAGES: Record<string, string> = {
  stock_market_ticker_unique: "その市場のティッカーは登録済み",
  stock_ticker_check:
    "ティッカーは半角の数字・英大文字・ピリオド・ハイフンだけ使える",
  stock_fiscal_month_market_check: "決算月はJP銘柄にだけ入れられる",
  stock_fiscal_month_check: "決算月は1〜12",
  holding_user_id_stock_id_pk: "その銘柄はすでに保有に登録済み",
};

/**
 * 登録を実行し、上の表にある制約違反なら日本語のエラー文を返す。
 * 表に無いエラーは投げ直す。握りつぶすと、理由が出ないまま失敗する画面になる
 */
async function run(operation: Promise<unknown>): Promise<string | null> {
  try {
    await operation;
  } catch (error) {
    const message = MESSAGES[violatedConstraint(error) ?? ""];
    if (message) {
      return message;
    }
    throw error;
  }
  return null;
}

export type StockInput = {
  market: "JP" | "US";
  ticker: string;
  name: string;
  /** 決算月（1〜12）。JP銘柄のみ。US銘柄では null（全体設計書 §4.1） */
  fiscalMonth: number | null;
};

/** 銘柄を登録する。成功で null、制約違反で日本語のエラー文を返す */
export function createStock(input: StockInput): Promise<string | null> {
  return run(db.insert(stock).values(input));
}

/**
 * 保有を登録する。成功で null、制約違反で日本語のエラー文を返す。
 * userId はセッションから渡す。この関数はセッションを読まない（設計書 §4）
 */
export function createHolding(
  userId: string,
  stockId: number,
): Promise<string | null> {
  return run(db.insert(holding).values({ userId, stockId }));
}
```

- [ ] **手順7: テストが通ることを確かめる**

```bash
pnpm vitest run src/db/register.test.ts
```

期待: 8件すべて PASS。

- [ ] **手順8: 全体のテストと型検査を流す**

```bash
pnpm test:run && pnpm typecheck && pnpm lint
```

期待: 3つとも exit 0。

- [ ] **手順9: コミット**

```bash
git add server/src/db/violation.ts server/src/db/register.ts server/src/db/register.test.ts server/test/helpers.ts
git commit -m "[feat] 銘柄と保有の登録処理を書く"
```

---

## タスク4: 管理画面の本体

**成果物**: `/` に銘柄フォーム・銘柄一覧・保有フォーム・保有一覧が出て、登録できる。未認証なら `/signin` に飛ぶ。

**ファイル**:
- 新規: `server/app/page.tsx`
- 新規: `server/app/stock-form.tsx`
- 新規: `server/app/holding-form.tsx`
- 変更: `server/app/actions.ts`（`addStock` と `addHolding` を足す）

**このタスクが使う名前**:
- タスク3の `createStock(input: StockInput)` と `createHolding(userId, stockId)`
- タスク2の `app/actions.ts`（同じファイルに足す）

- [ ] **手順1: `server/app/actions.ts` に登録の Server Action を足す**

ファイル先頭の import に足す。

```ts
import { revalidatePath } from "next/cache";
import { createHolding, createStock } from "../src/db/register";
```

`signIn` の下に次を足す。

```ts
/**
 * セッションを確かめて利用者IDを返す。
 * Server Action は画面を通さず直接POSTできるため、画面側の確認とは別にここでも確かめる
 * （Next.js 同梱ドキュメント 01-app/01-getting-started/07-mutating-data.md の警告）
 */
async function requireUserId(): Promise<string> {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) {
    redirect("/signin");
  }
  return session.user.id;
}

/**
 * 決算月の入力を読む。
 * フォームの空欄は FormData で "" になり、そのまま smallint に入れると
 * 制約違反ではない型変換エラーで 500 になる（設計書 §7 A）
 */
function toFiscalMonth(value: FormDataEntryValue | null): number | null {
  const text = String(value ?? "");
  return text === "" ? null : Number(text);
}

/** 銘柄を登録する。戻り値は失敗したときのエラー文で、useActionState の状態になる */
export async function addStock(
  _previous: string | null,
  formData: FormData,
): Promise<string | null> {
  await requireUserId();

  const message = await createStock({
    // <select> の選択肢は JP と US だけ。それ以外が来たら JP として扱わず弾く
    market: formData.get("market") === "US" ? "US" : "JP",
    ticker: String(formData.get("ticker") ?? ""),
    name: String(formData.get("name") ?? ""),
    fiscalMonth: toFiscalMonth(formData.get("fiscalMonth")),
  });
  if (message) {
    return message;
  }

  revalidatePath("/");
  return null;
}

/** 保有を登録する。戻り値は失敗したときのエラー文で、useActionState の状態になる */
export async function addHolding(
  _previous: string | null,
  formData: FormData,
): Promise<string | null> {
  const userId = await requireUserId();

  const message = await createHolding(
    userId,
    Number(formData.get("stockId")),
  );
  if (message) {
    return message;
  }

  revalidatePath("/");
  return null;
}
```

- [ ] **手順2: `server/app/stock-form.tsx` を作る**

```tsx
"use client";

import { useActionState } from "react";
import { addStock } from "./actions";

/** 決算月の選択肢（1〜12） */
const MONTHS = Array.from({ length: 12 }, (_, i) => i + 1);

/**
 * 銘柄の登録フォーム。
 * name は notNull だが空文字を弾く CHECK が無いため、required で塞ぐ（設計書 §3）
 */
export function StockForm() {
  const [error, formAction, pending] = useActionState(addStock, null);

  return (
    <form action={formAction} className="flex flex-col gap-3">
      <label className="flex flex-col gap-1">
        市場
        <select name="market" className="rounded border border-border p-2">
          <option value="JP">JP</option>
          <option value="US">US</option>
        </select>
      </label>
      <label className="flex flex-col gap-1">
        ティッカー
        <input
          type="text"
          name="ticker"
          required
          className="rounded border border-border p-2"
        />
      </label>
      <label className="flex flex-col gap-1">
        銘柄名
        <input
          type="text"
          name="name"
          required
          className="rounded border border-border p-2"
        />
      </label>
      <label className="flex flex-col gap-1">
        決算月（JP銘柄のみ。US銘柄は空のまま）
        <select name="fiscalMonth" className="rounded border border-border p-2">
          <option value="">なし</option>
          {MONTHS.map((month) => (
            <option key={month} value={month}>
              {month}
            </option>
          ))}
        </select>
      </label>
      <button
        type="submit"
        disabled={pending}
        className="rounded border border-border p-2 disabled:opacity-50"
      >
        {pending ? "送信中" : "銘柄を登録"}
      </button>
      {error && <p className="text-error">{error}</p>}
    </form>
  );
}
```

- [ ] **手順3: `server/app/holding-form.tsx` を作る**

```tsx
"use client";

import { useActionState } from "react";
import { addHolding } from "./actions";

type Choice = { id: number; market: string; ticker: string; name: string };

/**
 * 保有の登録フォーム。
 * user_id はセッションから取るため入力欄を出さない（設計書 §3）
 */
export function HoldingForm({ choices }: { choices: Choice[] }) {
  const [error, formAction, pending] = useActionState(addHolding, null);

  if (choices.length === 0) {
    return <p className="text-muted">先に銘柄を登録すると選べるようになる。</p>;
  }

  return (
    <form action={formAction} className="flex flex-col gap-3">
      <label className="flex flex-col gap-1">
        銘柄
        <select name="stockId" className="rounded border border-border p-2">
          {choices.map((choice) => (
            <option key={choice.id} value={choice.id}>
              {choice.market} {choice.ticker} {choice.name}
            </option>
          ))}
        </select>
      </label>
      <button
        type="submit"
        disabled={pending}
        className="rounded border border-border p-2 disabled:opacity-50"
      >
        {pending ? "送信中" : "保有を登録"}
      </button>
      {error && <p className="text-error">{error}</p>}
    </form>
  );
}
```

- [ ] **手順4: `server/app/page.tsx` を作る**

```tsx
import { eq } from "drizzle-orm";
import { headers } from "next/headers";
import { redirect } from "next/navigation";
import { auth } from "../src/auth";
import { db } from "../src/db";
import { holding, stock } from "../src/db/schema";
import { HoldingForm } from "./holding-form";
import { StockForm } from "./stock-form";

/**
 * 管理画面。銘柄と保有の登録フォームと一覧を縦に並べる（設計書 §3）。
 * 削除・編集・並べ替え・絞り込みは付けない
 */
export default async function Page() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) {
    redirect("/signin");
  }

  const stocks = await db
    .select()
    .from(stock)
    .orderBy(stock.market, stock.ticker);

  // 保有はサインインしている利用者の分だけを出す（設計書 §3）
  const holdings = await db
    .select({
      market: stock.market,
      ticker: stock.ticker,
      name: stock.name,
    })
    .from(holding)
    .innerJoin(stock, eq(holding.stockId, stock.id))
    .where(eq(holding.userId, session.user.id))
    .orderBy(stock.market, stock.ticker);

  return (
    <>
      <h1 className="text-xl font-bold">イチカブ 管理</h1>

      <section className="flex flex-col gap-3">
        <h2 className="text-base font-bold">銘柄を登録</h2>
        <StockForm />
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="text-base font-bold">銘柄一覧（{stocks.length}件）</h2>
        <ul className="flex flex-col gap-1">
          {stocks.map((row) => (
            <li key={row.id} className="border-b border-border py-1">
              {row.market} {row.ticker} {row.name}
              {row.fiscalMonth !== null && (
                <span className="text-muted"> / {row.fiscalMonth}月決算</span>
              )}
            </li>
          ))}
        </ul>
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="text-base font-bold">保有を登録</h2>
        <HoldingForm choices={stocks} />
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="text-base font-bold">保有一覧（{holdings.length}件）</h2>
        <ul className="flex flex-col gap-1">
          {holdings.map((row) => (
            <li
              key={`${row.market}-${row.ticker}`}
              className="border-b border-border py-1"
            >
              {row.market} {row.ticker} {row.name}
            </li>
          ))}
        </ul>
      </section>
    </>
  );
}
```

- [ ] **手順5: ビルド・型検査・整形を流す**

```bash
pnpm build && pnpm typecheck && pnpm lint
```

期待: 3つとも exit 0。

- [ ] **手順6: フォーム3つを見比べて、括り出すかを決める**

設計書 §8「共通のフォーム部品を先に作らない」に対する判断をここで下す。`signin-form.tsx` / `stock-form.tsx` / `holding-form.tsx` を並べて見る。

| 重なっているもの | 判断 |
|---|---|
| `useActionState` の呼び出し・送信ボタン・エラー行の3つだけ | **括り出さない**。枠に渡す引数（action・ボタンの文言・入力欄）が、重なっている行数とほぼ同じになる |
| 入力欄の組み立てまで重なっている | 括り出す |

判断した結果を、この手順のチェックボックスの下に1行で書き残す。

- [ ] **手順7: コミット**

```bash
git add server/app/page.tsx server/app/stock-form.tsx server/app/holding-form.tsx server/app/actions.ts
git commit -m "[feat] 銘柄と保有の登録画面を作る"
```

---

## タスク5: 目視確認と品質ゲート

**成果物**: 設計書 §10 の8手順の結果が設計書に書かれ、品質ゲートが exit 0 になる。

**ファイル**:
- 変更: `docs/records/specs/2026-08-09-6-admin-ui-design.md`（§10 に結果を追記）

- [ ] **手順1: DBを作り直して確認用データを入れる**

```bash
docker compose up -d --wait && pnpm db:migrate && pnpm db:seed
```

- [ ] **手順2: 開発サーバーを起動する**

```bash
pnpm dev
```

- [ ] **手順3: 設計書 §10 の8手順を実行する**

ブラウザのシークレットウィンドウで行う。結果を表に控える。

| # | 操作 | 期待 |
|---|---|---|
| 1 | `http://localhost:3000/` を開く | `/signin` に移る |
| 2 | `SEED_USER_EMAIL` / `SEED_USER_PASSWORD` でサインイン | `/` に移り、seed の3銘柄が銘柄一覧に出る |
| 3 | 誤ったパスワードでサインイン | 「メールアドレスまたはパスワードが違います」が出る |
| 4 | 銘柄フォームで JP・`7203`・トヨタ自動車・3月 を登録 | 「その市場のティッカーは登録済み」が出る。500 にならない |
| 5 | 銘柄フォームで US・`AAPL`・Apple・決算月なし を登録 | 銘柄一覧に増える。500 にならない |
| 6 | 銘柄フォームで JP・`130A`・任意の名前・12月 を登録 | 銘柄一覧に増える |
| 7 | 保有フォームで `AAPL` を選んで登録 | 保有一覧に増える |
| 8 | もう一度 `AAPL` を保有に登録 | 「その銘柄はすでに保有に登録済み」が出る |

手順5 が 500 になった場合は設計書 §7 A の対処（`""` → `null`）が効いていない。手順1 が `/signin` に移らない場合は `page.tsx` のセッション確認が効いていない。どちらも先に進まず報告する。

- [ ] **手順4: 結果を設計書の §10 に追記する**

`### 結果（実施日: YYYY-MM-DD）` の見出しを立て、手順3 の表に「結果」の列を足したものを書く。Issue #8 設計書 §7 手順4 と同じ形式にする。

- [ ] **手順5: 品質ゲートを流す**

テストがDBを空にするため、`pnpm dev` を止めてから流す。

```bash
pnpm install && pnpm gen && pnpm build && pnpm test:run && pnpm typecheck && pnpm lint
```

期待: すべて exit 0。**warning も 0 件**にする。既存の問題であっても直す。

- [ ] **手順6: iOS 側のゲートを流す**

このIssueは `openapi.yaml` を変えていないため iOS への影響は無いはずだが、`CLAUDE.md` の品質ゲートは2系統ある。`ios/` で流す。

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `TEST SUCCEEDED`。

- [ ] **手順7: コミット**

```bash
git add docs/records/specs/2026-08-09-6-admin-ui-design.md
git commit -m "[docs] 管理UIの目視確認の結果を設計書に書く"
```

- [ ] **手順8: 最新の main を取り込む**

PR を作る前に、コンフリクトを先に解消する（開発プロセス ステップ4）。

```bash
git fetch origin main && git merge origin/main
```

- [ ] **手順9: 取り込み後にもう一度ゲートを流す**

main の変更が入った状態で通ることを確かめる。手順5 と同じコマンドを流す。

```bash
pnpm install && pnpm gen && pnpm build && pnpm test:run && pnpm typecheck && pnpm lint
```

期待: すべて exit 0。

---

## この計画でやらないこと

| 対象 | 理由 |
|---|---|
| `theme` / `theme_stock` の登録画面 | タスク1 の手順4 で作る別Issue |
| イベントの登録画面 | Issue #7 |
| 一覧の削除・編集・並べ替え・絞り込み・ページ送り | 設計書 §3 |
| サインアウト | 設計書 §12。Cookie を消せば足りる |
| `app/error.tsx` / `app/not-found.tsx` | 完了条件に無い。必要になったら足す |
| Server Action の自動テスト | `next/headers` を使うため Vitest から呼べない。テスト対象は `src/db/register.ts`（設計書 §4・§9） |
| `stock.name` の空文字を弾く CHECK 制約 | HTML の `required` で塞ぐ（設計書 §3・§12） |
| `src/db/seed-event.ts` の変更 | そのまま残す（設計書 §2）。iOS の確認用データを戻す手段として使い続ける |

## タスク5 のあと

開発プロセスのステップ8以降（PR作成 → レビュー対応 → マージ → worktree の後始末）に進む。この計画の範囲はタスク5まで。
