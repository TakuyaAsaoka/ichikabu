# iOS サインインとイベント取得 実装計画

> **実装する人へ**: 必ず `superpowers:subagent-driven-development`（推奨）か `superpowers:executing-plans` を使い、タスク単位で進めること。手順はチェックボックス（`- [ ]`）で追える形にしてある。

**ゴール**: iOS アプリでサインインし、Bearer トークンを Keychain に保存して `GET /api/events` を全件取得し、素朴な `List` にタイトルを並べる。401 が返ったらサインイン画面に戻す。

**進め方**: 通信は URLSession + async/await で自分で書き、`openapi.yaml` からは型だけを生成する。応答の判定（401 か、読み取れるか）は引数だけで動く関数に切り出し、通信をせずにテストする。画面は「トークンがあるか」だけで出し分ける。

**使うもの**: Swift 6 / SwiftUI / Swift Testing / Security フレームワーク（Keychain）/ swift-openapi-generator（型のみ）/ Next.js + Drizzle（seed の追加のみ）

**根拠**: [設計書](../specs/2026-08-08-5-ios-signin-design.md)。用語や背景は [全体設計書](../specs/2026-08-02-1-ichikabu-design.md) を見る。

## 全体を通じた決まりごと

- **言語**: コードコメント・テストの表示名・コミットメッセージは日本語。ファイル名・型名・関数名は英語
- **コミットメッセージ**: `[feat] ...` `[fix] ...` `[chore] ...` の形。末尾に次の2行を付ける

  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01Hz42Gqwd9M6wHAbYaqxJKY
  ```
- **Swift のインデントはタブ**（既存ファイルに合わせる）。TypeScript は Biome に従う（半角スペース2つ）
- **iOS の品質ゲート**（`ios/` で実行）

  ```
  xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
  ```
- **server の品質ゲート**（`server/` で実行。開発用DBが起動していること）

  ```
  pnpm install && pnpm gen && pnpm build && pnpm test:run && pnpm typecheck && pnpm lint
  ```
- **エラーも警告も0件にする**。既存のものでも直す。ただし `appintentsmetadataprocessor ... No AppIntents.framework dependency found.` は Apple のツールが必ず出すもので消せない（iOS骨組み設計書 §3）
- **`ios/Ichikabu/` はXcodeの同期フォルダ**なので、ファイルを置く・消すだけでターゲットに反映される。`project.pbxproj` を編集する必要はない
- **開発用サーバーの起動**（動作確認に使う。`server/` で実行）

  ```
  docker compose up -d --wait && pnpm dev
  ```

## 作るファイル

| ファイル | 役割 |
|---|---|
| `server/src/db/seed-event.ts` | 確認用のイベントを投入する（新規） |
| `server/scripts/seed.ts` | 上を呼ぶ（修正） |
| `ios/IchikabuAPI/Sources/IchikabuAPI/openapi-generator-config.yaml` | 生成を型だけにする（修正） |
| `ios/IchikabuAPI/Sources/IchikabuAPI/Contract.swift` | `Event` の別名を足す（修正） |
| `ios/IchikabuAPI/Package.swift` | `swift-http-types` の依存を外す（修正。外せる場合のみ） |
| `ios/Ichikabu/APIClient.swift` | 通信と応答の判定（新規） |
| `ios/Ichikabu/TokenStore.swift` | Keychain の読み書き（新規） |
| `ios/Ichikabu/SignInView.swift` | サインイン画面（新規） |
| `ios/Ichikabu/EventListView.swift` | イベント一覧（新規） |
| `ios/Ichikabu/IchikabuApp.swift` | 画面の出し分け（修正） |
| `ios/Ichikabu/ContentView.swift` | 役目を終えたので削除 |
| `ios/IchikabuTests/APIClientTests.swift` | 応答の判定のテスト（新規） |
| `ios/IchikabuTests/TokenStoreTests.swift` | Keychain のテスト（新規） |

---

### タスク1: 確認用のイベントを seed に足す（server）

**ファイル:**
- 新規: `server/src/db/seed-event.ts`
- 修正: `server/scripts/seed.ts`

**このタスクが作るもの:** `seedEvents(): Promise<{ created: number }>`。市場が `GLOBAL` のイベント3件を投入する。何度実行しても増えない。

**自動テストを書かない理由:** 既存の `src/db/seed-user.ts` にもテストが無く、投入スクリプトは実行そのものが確認になる。ここだけ流儀を変えない。

- [ ] **手順1: `server/src/db/seed-event.ts` を作る**

```typescript
import { inArray } from "drizzle-orm";
import { db } from ".";
import { event } from "./schema";

/**
 * 開発中の表示確認に使うイベント（Issue #5 設計書 §3 判断4）。
 * 市場が GLOBAL のイベントは保有銘柄が無くても全員に返るため、
 * 銘柄・テーマを作らずに iOS の一覧表示を確認できる。
 */
const EVENTS: (typeof event.$inferInsert)[] = [
  {
    market: "GLOBAL",
    title: "FOMC 政策金利発表",
    shortLabel: "FOMC",
    startDate: "2026-09-16",
    endDate: "2026-09-17",
    time: "03:00:00",
    importance: 3,
  },
  {
    market: "GLOBAL",
    title: "米消費者物価指数（CPI）",
    shortLabel: "米CPI",
    startDate: "2026-09-11",
    time: "21:30:00",
    importance: 2,
  },
  {
    market: "GLOBAL",
    title: "米雇用統計",
    shortLabel: "米雇用",
    startDate: "2026-10-02",
    time: "21:30:00",
    importance: 3,
    note: "非農業部門雇用者数と失業率",
  },
];

/**
 * イベントを投入する。何度実行しても増えない。
 * イベントには一意の制約が無いため、見出しで既にあるかを判定する。
 */
export async function seedEvents(): Promise<{ created: number }> {
  const existing = await db
    .select({ title: event.title })
    .from(event)
    .where(
      inArray(
        event.title,
        EVENTS.map((e) => e.title),
      ),
    );
  const have = new Set(existing.map((row) => row.title));
  const missing = EVENTS.filter((e) => !have.has(e.title));
  if (missing.length > 0) await db.insert(event).values(missing);
  return { created: missing.length };
}
```

- [ ] **手順2: `server/scripts/seed.ts` から呼ぶ**

`process.exit(0)` の直前に足す。ファイル冒頭の import も足す。

```typescript
import { seedUser } from "../src/db/seed-user";
import { seedEvents } from "../src/db/seed-event";
```

```typescript
const { created: eventCount } = await seedEvents();
console.log(`イベントを ${eventCount} 件作成した`);
```

- [ ] **手順3: 実行して3件入ることを確かめる**

`server/` で実行:

```bash
pnpm db:seed
```

期待する出力: `イベントを 3 件作成した`

- [ ] **手順4: もう一度実行して増えないことを確かめる**

```bash
pnpm db:seed
```

期待する出力: `イベントを 0 件作成した`

- [ ] **手順5: APIから3件返ることを確かめる**

`server/` で `pnpm dev` を起動しておき、別の端末で実行する（メールとパスワードは `.env.local` の `SEED_USER_EMAIL` / `SEED_USER_PASSWORD`）:

```bash
TOKEN=$(curl -s -D - -o /dev/null -X POST http://localhost:3000/api/auth/sign-in/email \
  -H 'content-type: application/json' \
  -d '{"email":"<SEED_USER_EMAIL>","password":"<SEED_USER_PASSWORD>"}' \
  | grep -i '^set-auth-token:' | cut -d' ' -f2 | tr -d '\r')
curl -s http://localhost:3000/api/events -H "authorization: Bearer $TOKEN"
```

期待する出力: 3件の配列が返り、`"kind":"market"` と `"title":"FOMC 政策金利発表"` を含む。

- [ ] **手順6: server の品質ゲートを流す**

`server/` で実行:

```bash
pnpm install && pnpm gen && pnpm build && pnpm test:run && pnpm typecheck && pnpm lint
```

期待: 全部成功。警告も0件。

- [ ] **手順7: コミット**

```bash
git add server/src/db/seed-event.ts server/scripts/seed.ts
git commit -m "$(cat <<'EOF'
[feat] 確認用のイベントを seed に足す

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Hz42Gqwd9M6wHAbYaqxJKY
EOF
)"
```

---

### タスク2: 契約から作るものを型だけにする（iOS）

**ファイル:**
- 修正: `ios/IchikabuAPI/Sources/IchikabuAPI/openapi-generator-config.yaml`
- 修正: `ios/IchikabuAPI/Sources/IchikabuAPI/Contract.swift`
- 修正: `ios/IchikabuAPI/Package.swift`

**このタスクが作るもの:** `IchikabuAPI.Event`（`Components.Schemas.Event` の別名）。以降のタスクはこの名前で参照する。イベントの項目は `id: Int` / `kind: Event.kindPayload`（`.market` `.theme` `.stock`）/ `title: String` / `shortLabel: String` / `startDate: String` / `endDate: String?` / `time: String?` / `importance: Int` / `note: String?`。

- [ ] **手順1: 生成の設定から `client` を外す**

`ios/IchikabuAPI/Sources/IchikabuAPI/openapi-generator-config.yaml` の全体を次に置き換える。

```yaml
generate:
  - types
# 通信は URLSession で自分で書くため client は生成しない（Issue #5 設計書 §3 判断3）。
# 経路が増えたら client を1行戻せばよい
# アプリ本体とテストから生成された型を使うため public にする
accessModifier: public
```

- [ ] **手順2: `Contract.swift` に `Event` の別名を足す**

ファイル末尾に足す。

```swift
/// `GET /api/events` が返すイベント
public typealias Event = Components.Schemas.Event
```

- [ ] **手順3: ビルドが通ることを確かめる**

`ios/` で実行:

```bash
xcodebuild build -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `BUILD SUCCEEDED`。

- [ ] **手順4: `swift-http-types` の依存を外せるか試す**

`ios/IchikabuAPI/Package.swift` から次の2か所を消す。

```swift
		// 生成されたクライアントが HTTPTypes の型を直接使う。
		// swift-openapi-runtime 経由の間接依存だけでは、フレームワークのリンク時に見つからない
		.package(url: "https://github.com/apple/swift-http-types", from: "1.6.0"),
```

```swift
				.product(name: "HTTPTypes", package: "swift-http-types"),
```

- [ ] **手順5: 外した状態でビルドできるか確かめる**

`ios/` で実行:

```bash
xcodebuild build -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

`BUILD SUCCEEDED` なら外したままにする。`Undefined symbol: HTTPTypes...` などで落ちたら手順4の変更を元に戻し、`Package.swift` のコメントを次に書き換える。

```swift
		// 型だけの生成でも HTTPTypes の型が現れるため、明示的な依存が要る。
		// 間接依存のままだとフレームワークのリンク時に見つからない
```

- [ ] **手順6: コミット**

```bash
git add ios/IchikabuAPI
git commit -m "$(cat <<'EOF'
[chore] 契約から作るものを型だけにする

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Hz42Gqwd9M6wHAbYaqxJKY
EOF
)"
```

---

### タスク3: 応答の判定と通信（APIClient）

**ファイル:**
- 新規: `ios/Ichikabu/APIClient.swift`
- 新規テスト: `ios/IchikabuTests/APIClientTests.swift`

**このタスクが使うもの:** タスク2の `IchikabuAPI.Event`。

**このタスクが作るもの:**
- `enum APIError: Error, Equatable`（`.unauthorized` / `.missingToken` / `.notHTTP` / `.unexpectedStatus(Int)`）
- `struct APIClient`
  - `func signIn(email: String, password: String) async throws -> String`（トークンを返す）
  - `func events(token: String) async throws -> [Event]`
  - `static func token(from response: URLResponse) throws -> String`
  - `static func events(from data: Data, response: URLResponse) throws -> [Event]`

- [ ] **手順1: 失敗するテストを書く**

`ios/IchikabuTests/APIClientTests.swift` を新規作成する。

```swift
import Foundation
import IchikabuAPI
import Testing

@testable import Ichikabu

@Suite("APIの応答の判定")
struct APIClientTests {
	/// 判定はステータスとヘッダしか見ないため、URLは何でもよい
	private static let url = URL(string: "http://localhost:3000/api/events")!

	private func response(status: Int, headers: [String: String] = [:]) -> HTTPURLResponse {
		HTTPURLResponse(url: Self.url, statusCode: status, httpVersion: nil, headerFields: headers)!
	}

	@Test("401 は認証切れとして扱う")
	func unauthorized() {
		#expect(throws: APIError.unauthorized) {
			try APIClient.events(from: Data(), response: response(status: 401))
		}
	}

	@Test("サーバーが返す形の JSON をイベントに読み取れる")
	func decodeEvents() throws {
		let json = Data(
			"""
			[{"id":1,"kind":"market","title":"FOMC 政策金利発表","shortLabel":"FOMC",\
			"startDate":"2026-09-16","endDate":"2026-09-17","time":"03:00:00",\
			"importance":3,"note":null}]
			""".utf8)
		let events = try APIClient.events(from: json, response: response(status: 200))
		#expect(events.count == 1)
		#expect(events[0].kind == .market)
		#expect(events[0].title == "FOMC 政策金利発表")
		#expect(events[0].endDate == "2026-09-17")
		#expect(events[0].note == nil)
	}

	@Test("サインインの応答ヘッダからトークンを取り出す")
	func extractToken() throws {
		let token = try APIClient.token(
			from: response(status: 200, headers: ["set-auth-token": "abc.def"]))
		#expect(token == "abc.def")
	}

	@Test("サインインに失敗すると認証切れとして扱う")
	func signInFailure() {
		#expect(throws: APIError.unauthorized) {
			try APIClient.token(from: response(status: 401))
		}
	}
}
```

- [ ] **手順2: 落ちることを確かめる**

`ios/` で実行:

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `cannot find 'APIClient' in scope` でコンパイルに失敗する。

- [ ] **手順3: `ios/Ichikabu/APIClient.swift` を書く**

```swift
import Foundation
import IchikabuAPI

/// 通信の失敗
enum APIError: Error, Equatable {
	/// 認証切れ。サインイン画面に戻す
	case unauthorized
	/// 成功したのに set-auth-token が付いていない
	case missingToken
	/// HTTP の応答ではない
	case notHTTP
	/// 想定していないステータス
	case unexpectedStatus(Int)
}

/// サーバーとの通信。
/// 経路が2本しかないため、契約からは型だけを生成し、通信は自分で書く（Issue #5 設計書 §3 判断3）。
struct APIClient {
	static let baseURL = URL(string: "http://localhost:3000")!

	/// サインインしてトークンを返す。
	/// この経路は Better Auth のもので openapi.yaml に載せていない（Issue #5 設計書 §3 判断1）
	func signIn(email: String, password: String) async throws -> String {
		var request = URLRequest(url: Self.baseURL.appending(path: "/api/auth/sign-in/email"))
		request.httpMethod = "POST"
		request.setValue("application/json", forHTTPHeaderField: "content-type")
		request.httpBody = try JSONEncoder().encode(["email": email, "password": password])
		let (_, response) = try await URLSession.shared.data(for: request)
		return try Self.token(from: response)
	}

	/// 保有に対応するイベントを全件取る
	func events(token: String) async throws -> [Event] {
		var request = URLRequest(url: Self.baseURL.appending(path: "/api/events"))
		request.setValue("Bearer \(token)", forHTTPHeaderField: "authorization")
		let (data, response) = try await URLSession.shared.data(for: request)
		return try Self.events(from: data, response: response)
	}

	// 以下は引数だけで動く。通信をせずにテストできるように分けている

	/// 応答ヘッダからトークンを取り出す
	static func token(from response: URLResponse) throws -> String {
		let http = try verified(response)
		guard let token = http.value(forHTTPHeaderField: "set-auth-token") else {
			throw APIError.missingToken
		}
		return token
	}

	/// 応答をイベントの配列に読み取る
	static func events(from data: Data, response: URLResponse) throws -> [Event] {
		_ = try verified(response)
		return try JSONDecoder().decode([Event].self, from: data)
	}

	/// 成功の応答だけを通す
	private static func verified(_ response: URLResponse) throws -> HTTPURLResponse {
		guard let http = response as? HTTPURLResponse else { throw APIError.notHTTP }
		if http.statusCode == 401 { throw APIError.unauthorized }
		guard (200..<300).contains(http.statusCode) else {
			throw APIError.unexpectedStatus(http.statusCode)
		}
		return http
	}
}
```

- [ ] **手順4: テストが通ることを確かめる**

`ios/` で実行:

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `TEST SUCCEEDED`。警告も0件。

- [ ] **手順5: コミット**

```bash
git add ios/Ichikabu/APIClient.swift ios/IchikabuTests/APIClientTests.swift
git commit -m "$(cat <<'EOF'
[feat] サインインとイベント取得の通信を作る

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Hz42Gqwd9M6wHAbYaqxJKY
EOF
)"
```

---

### タスク4: トークンの保管（TokenStore）

**ファイル:**
- 新規: `ios/Ichikabu/TokenStore.swift`
- 新規テスト: `ios/IchikabuTests/TokenStoreTests.swift`

**このタスクが作るもの:** `enum TokenStore` の3つ。`static func save(_ token: String)` / `static func load() -> String?` / `static func delete()`。

- [ ] **手順1: 失敗するテストを書く**

`ios/IchikabuTests/TokenStoreTests.swift` を新規作成する。

```swift
import Testing

@testable import Ichikabu

@Suite("トークンの保管")
struct TokenStoreTests {
	@Test("保存したトークンを読み出せる。消すと読めなくなる")
	func saveLoadDelete() {
		TokenStore.save("test-token")
		#expect(TokenStore.load() == "test-token")

		// 上書きできる（サインインし直した場合）
		TokenStore.save("test-token-2")
		#expect(TokenStore.load() == "test-token-2")

		TokenStore.delete()
		#expect(TokenStore.load() == nil)
	}
}
```

- [ ] **手順2: 落ちることを確かめる**

`ios/` で実行:

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `cannot find 'TokenStore' in scope` でコンパイルに失敗する。

- [ ] **手順3: `ios/Ichikabu/TokenStore.swift` を書く**

```swift
import Foundation
import Security

/// サインインのトークンを Keychain に読み書きする（全体設計書 §9）。
/// ラッパーのライブラリは入れず、Security フレームワークをそのまま使う。
enum TokenStore {
	private static let service = "com.takuyaasaoka.ichikabu"
	private static let account = "session-token"

	/// 項目を1つに特定するための条件
	private static var query: [String: Any] {
		[
			kSecClass as String: kSecClassGenericPassword,
			kSecAttrService as String: service,
			kSecAttrAccount as String: account,
		]
	}

	static func save(_ token: String) {
		// 同じ条件の項目があると追加が失敗するため、先に消してから入れる
		SecItemDelete(query as CFDictionary)
		var attributes = query
		attributes[kSecValueData as String] = Data(token.utf8)
		SecItemAdd(attributes as CFDictionary, nil)
	}

	static func load() -> String? {
		var lookup = query
		lookup[kSecReturnData as String] = true
		lookup[kSecMatchLimit as String] = kSecMatchLimitOne
		var item: CFTypeRef?
		guard SecItemCopyMatching(lookup as CFDictionary, &item) == errSecSuccess,
			let data = item as? Data
		else { return nil }
		return String(decoding: data, as: UTF8.self)
	}

	static func delete() {
		SecItemDelete(query as CFDictionary)
	}
}
```

- [ ] **手順4: テストが通ることを確かめる**

`ios/` で実行:

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `TEST SUCCEEDED`。

もし `-34018`（`errSecMissingEntitlement`）で落ちた場合は、テストの実行環境が Keychain を触れていない。その場合は自分で回避策を作らず、落ちた出力をそのまま報告して指示を仰ぐこと。

- [ ] **手順5: コミット**

```bash
git add ios/Ichikabu/TokenStore.swift ios/IchikabuTests/TokenStoreTests.swift
git commit -m "$(cat <<'EOF'
[feat] トークンを Keychain に保管する

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Hz42Gqwd9M6wHAbYaqxJKY
EOF
)"
```

---

### タスク5: 画面をつなぐ

**ファイル:**
- 新規: `ios/Ichikabu/SignInView.swift`
- 新規: `ios/Ichikabu/EventListView.swift`
- 修正: `ios/Ichikabu/IchikabuApp.swift`
- 削除: `ios/Ichikabu/ContentView.swift`

**このタスクが使うもの:** タスク3の `APIClient` と `APIError`、タスク4の `TokenStore`、タスク2の `Event`。

**自動テストを書かない理由:** 画面の組み立てだけで、判定のロジックはタスク3・4で固定済み。動きの確認はタスク6で手で行う。

- [ ] **手順1: `ios/Ichikabu/SignInView.swift` を作る**

```swift
import SwiftUI

/// サインイン画面。成功したトークンを onSignedIn で渡す
struct SignInView: View {
	let onSignedIn: (String) -> Void

	@State private var email = ""
	@State private var password = ""
	@State private var message: String?

	var body: some View {
		Form {
			TextField("メールアドレス", text: $email)
				.textContentType(.emailAddress)
				.keyboardType(.emailAddress)
				.textInputAutocapitalization(.never)
				.autocorrectionDisabled()
			SecureField("パスワード", text: $password)
			Button("サインイン") {
				Task { await signIn() }
			}
			if let message {
				Text(message).foregroundStyle(.red)
			}
		}
	}

	private func signIn() async {
		do {
			let token = try await APIClient().signIn(email: email, password: password)
			onSignedIn(token)
		} catch APIError.unauthorized {
			message = "メールアドレスかパスワードが違います"
		} catch {
			message = "サインインできませんでした"
		}
	}
}

#Preview {
	SignInView(onSignedIn: { _ in })
}
```

- [ ] **手順2: `ios/Ichikabu/EventListView.swift` を作る**

```swift
import IchikabuAPI
import SwiftUI

/// イベント一覧。疎通の確認のための足場で、Issue #8 で月グリッドに置き換わる
struct EventListView: View {
	let token: String
	/// 401 が返ったときに呼ぶ
	let onUnauthorized: () -> Void

	@State private var events: [Event] = []
	@State private var message: String?

	var body: some View {
		NavigationStack {
			List(events, id: \.id) { event in
				Text(event.title)
			}
			.navigationTitle("イベント")
			.overlay {
				if let message {
					Text(message).foregroundStyle(.secondary)
				}
			}
		}
		.task {
			await load()
		}
	}

	private func load() async {
		do {
			events = try await APIClient().events(token: token)
		} catch APIError.unauthorized {
			onUnauthorized()
		} catch {
			message = "イベントを取得できませんでした"
		}
	}
}
```

- [ ] **手順3: `ios/Ichikabu/IchikabuApp.swift` を書き換える**

ファイル全体を次に置き換える。

```swift
import SwiftUI

@main
struct IchikabuApp: App {
	/// トークンの有無で画面を出し分ける。リフレッシュ機構は作らない（全体設計書 §7）
	@State private var token: String? = TokenStore.load()

	var body: some Scene {
		WindowGroup {
			if let token {
				EventListView(token: token, onUnauthorized: signOut)
			} else {
				SignInView(onSignedIn: signIn)
			}
		}
	}

	private func signIn(_ token: String) {
		TokenStore.save(token)
		self.token = token
	}

	private func signOut() {
		TokenStore.delete()
		token = nil
	}
}
```

- [ ] **手順4: `ContentView.swift` を消す**

```bash
git rm ios/Ichikabu/ContentView.swift
```

- [ ] **手順5: 品質ゲートを流す**

`ios/` で実行:

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: `TEST SUCCEEDED`。警告も0件。

- [ ] **手順6: コミット**

```bash
git add ios/Ichikabu
git commit -m "$(cat <<'EOF'
[feat] サインイン画面とイベント一覧をつなぐ

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Hz42Gqwd9M6wHAbYaqxJKY
EOF
)"
```

---

### タスク6: 実機（シミュレータ）で完了条件を確かめる

**ファイル:** 変更なし。確認だけを行う。

**前提:** `server/` で `docker compose up -d --wait` と `pnpm dev` が動いていること。タスク1の seed が投入済みであること。

- [ ] **手順1: シミュレータでアプリを起動する**

`ios/` で実行:

```bash
xcodebuild build -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation -derivedDataPath "$TMPDIR"/ichikabu-dd
xcrun simctl boot 'iPhone 17' || true
xcrun simctl install booted "$TMPDIR"/ichikabu-dd/Build/Products/Debug-iphonesimulator/Ichikabu.app
xcrun simctl launch booted com.takuyaasaoka.ichikabu
```

- [ ] **手順2: 完了条件1 — サインインすると3件並ぶ**

サインイン画面に `.env.local` の `SEED_USER_EMAIL` / `SEED_USER_PASSWORD` を入れて「サインイン」を押す。

期待: 一覧に `FOMC 政策金利発表` `米消費者物価指数（CPI）` `米雇用統計` の3件が並ぶ。

もし `App Transport Security policy requires the use of a secure connection` で通信が失敗した場合は、自分で回避策を作らず、その出力をそのまま報告して指示を仰ぐこと。

- [ ] **手順3: 完了条件2 — トークンが無い状態で起動するとサインイン画面**

アプリを消して入れ直す（Keychain も一緒に消える）。

```bash
xcrun simctl uninstall booted com.takuyaasaoka.ichikabu
xcrun simctl install booted "$TMPDIR"/ichikabu-dd/Build/Products/Debug-iphonesimulator/Ichikabu.app
xcrun simctl launch booted com.takuyaasaoka.ichikabu
```

期待: サインイン画面が出る。

- [ ] **手順4: 完了条件3 — 無効なトークンだと401でサインイン画面に戻る**

手順3のあとで一度サインインし、一覧が出た状態にする。その状態でサーバー側のセッションを消すと、アプリが持っているトークンが無効になる。

```bash
docker exec server-db-1 psql -U postgres -d ichikabu -c 'delete from "session";'
```

アプリを一度閉じて開き直す（シミュレータでアプリを終了 → 再度起動）。

```bash
xcrun simctl terminate booted com.takuyaasaoka.ichikabu
xcrun simctl launch booted com.takuyaasaoka.ichikabu
```

期待: 401 が返り、サインイン画面に戻る。

- [ ] **手順5: 両方の品質ゲートを最後にもう一度流す**

`server/` で実行:

```bash
pnpm install && pnpm gen && pnpm build && pnpm test:run && pnpm typecheck && pnpm lint
```

`ios/` で実行:

```bash
xcodebuild build test -scheme Ichikabu -destination 'platform=iOS Simulator,name=iPhone 17' -skipPackagePluginValidation
```

期待: どちらもエラー0件・警告0件。

- [ ] **手順6: 確認結果を報告する**

手順2〜4の3つについて、期待どおりだったかを1行ずつ書く。期待と違ったものがあれば、実際に何が起きたかを出力つきで書く。
